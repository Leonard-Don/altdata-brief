{
  "_comment": "Schema baseline for ETF 512400 liveSnapshot. Adapter pulls a flat normalized payload — the keys we depend on are listed here.",
  "baseline_version": 1,
  "source": "etf_512400",
  "expected_payload_keys": {
    "_root": {
      "required": ["price", "change_percent", "trade_date", "nav", "source_health"],
      "optional": ["generated_at", "mode", "code", "name", "amount_cny", "turnover_rate", "recent_nav", "commodity_drivers", "source_mode", "public_summary_path", "public_by_default"]
    },
    "nav": {
      "required": ["date"],
      "optional": ["unit", "daily_return", "accumulated"]
    },
    "source_health": {
      "required": ["total", "ok", "verdict"],
      "optional": ["required_total", "required_ok", "fallback", "sources", "required_degraded", "quote_ok", "quote_fallback"]
    }
  }
}
